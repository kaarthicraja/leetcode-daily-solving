# Smallest Stable Index II

Difficulty: Medium
Topic: Array, Prefix Sum
LeetCode: https://leetcode.com/problems/smallest-stable-index-ii/

## Problem

You are given an integer array nums of length n and an integer k.

For each index i, define its instability score as max(nums[0..i]) - min(nums[i..n - 1]).

In other words:

- max(nums[0..i]) is the largest value among the elements from index 0 to index i.

- min(nums[i..n - 1]) is the smallest value among the elements from index i to index n - 1.

An index i is called stable if its instability score is less than or equal to k.

Return the smallest stable index. If no such index exists, return -1.

 

Example 1:

Input: nums = [5,0,1,4], k = 3

Output: 3

Explanation:

- At index 0: The maximum in [5] is 5, and the minimum in [5, 0, 1, 4] is 0, so the instability score is 5 - 0 = 5.

- At index 1: The maximum in [5, 0] is 5, and the minimum in [0, 1, 4] is 0, so the instability score is 5 - 0 = 5.

- At index 2: The maximum in [5, 0, 1] is 5, and the minimum in [1, 4] is 1, so the instability score is 5 - 1 = 4.

- At index 3: The maximum in [5, 0, 1, 4] is 5, and the minimum in [4] is 4, so the instability score is 5 - 4 = 1.

- This is the first index with an instability score less than or equal to k = 3. Thus, the answer is 3.

Example 2:

Input: nums = [3,2,1], k = 1

Output: -1

Explanation:

- At index 0, the instability score is 3 - 1 = 2.

- At index 1, the instability score is 3 - 1 = 2.

- At index 2, the instability score is 3 - 1 = 2.

- None of these values is less than or equal to k = 1, so the answer is -1.

Example 3:

Input: nums = [0], k = 0

Output: 0

Explanation:

At index 0, the instability score is 0 - 0 = 0, which is less than or equal to k = 0. Therefore, the answer is 0.

 

Constraints:

- 1 <= nums.length <= 105

- 0 <= nums[i] <= 109

- 0 <= k <= 109

## Approach

Precompute suffix_min in one right-to-left pass. Then scan left to right, growing a running prefix_max as we go, and return the first index i where prefix_max - suffix_min[i] <= k.

**Brute-force alternative:** For every index i, recompute max(nums[0..i]) and min(nums[i..n-1]) directly: O(n) work per index, O(n^2) overall.

## Algorithm

1. Build suffix_min[i] = min(nums[i..n-1]) with one right-to-left pass.
2. Scan i from 0 to n-1, updating a running prefix_max = max(nums[0..i]) as we go.
3. At each i, check prefix_max - suffix_min[i] <= k; return i on the first match.
4. Return -1 if the scan finishes with no match.

## Complexity

Time: O(n)
Space: O(n) for the suffix_min array

## Edge Cases

- n == 1: instability score is always 0, and k >= 0, so index 0 always qualifies.
- k very large (>= global max - global min): index 0 always qualifies.
- No index qualifies: return -1.

## Tests

12/12 passed

## Key Learning

Checking index i requires prefix_max already including nums[i] -- update prefix_max before the comparison, not after, or index 0 (and every index's own value) gets skipped.

## Review

Score: 8.8/10

Strengths:
- Correct O(n) two-pass approach: suffix_min built right-to-left, then a single left-to-right scan with a running prefix_max
- Clean early return the moment the stability condition is met, avoiding unnecessary work
- Variable names (prefix_max, suffix_min) make the invariant being maintained easy to follow

Weaknesses:
- `prefix_max = nums[0]` before the loop is redundant — the loop's `max(prefix_max, nums[i])` at i=0 already yields the same result
- No guard for n == 0 (would raise IndexError on `nums[n-1]`); fine if constraints guarantee n >= 1, but worth confirming

Key takeaway: Precomputing a suffix aggregate array lets you combine it with a running prefix aggregate in one forward pass, turning an O(n^2) min/max-window search into O(n).

---

*Generated by DSA Daily Agent. This solution was prepared automatically; submitting it on LeetCode is always a manual step.*
